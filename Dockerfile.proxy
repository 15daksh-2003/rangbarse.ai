FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
COPY scripts/devServer.js ./scripts/
COPY server.config.example.json ./server.config.json

RUN npm ci --omit=dev --ignore-scripts

EXPOSE 3001

CMD ["node", "scripts/devServer.js"]
